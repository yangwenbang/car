<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.car.dao.CategoryDao">

	<select id="findCategoryByCategoryId" resultType="com.car.vo.CategoryVO">
		SELECT id as category_id,project_id,category_name,category_name,category_picture
		from category where id = #{categoryId}
	</select>
	
</mapper>