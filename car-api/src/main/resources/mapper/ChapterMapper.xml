<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.car.dao.ChapterDao">

	<select id="getChaptersByCourse" resultType="com.car.vo.ChapterVO">
		SELECT id AS chapterId,course_id,chapter_name,description
		FROM chapter WHERE course_id = #{courseId} order by seq
	</select>

</mapper>