<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.car.dao.VipContentDao">

	<select id="getVipContentsByProject" resultType="com.car.vo.VipContentVO">
		SELECT id vipContentId,vip_content,vip_day,vip_money 
		FROM vip_content
		WHERE project_id = #{projectId}
		ORDER BY seq
	</select>
	
	<select id="findByPK" resultType="com.car.vo.VipContentVO">
		SELECT id vipContentId,vip_content,vip_day,vip_money 
		FROM vip_content
		WHERE id = #{vipContentId}
	</select>
	
</mapper>